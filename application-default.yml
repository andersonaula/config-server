spring:
  application:
    name: greeting-service

  cloud:
    config:
      enabled: false
      import-check:
        enabled: false


greeting-service:
  greeting: "Ciao"
  default-value: "Mondo"


management:
  endpoints:
    web:
      exposure:
        include: '*'
